Resume file is now downloaded from the database (used to be a static web resource).

----------

Resume file is now downloaded from the database (used to be a static web resource):

-Getting deeper into Java I/O operations, reading/writing raw byte data. Now that I
have good understanding of this process, I feel like this should be the time to move
the Resume file to the database as it is frequently updated, commiting in Git every
time the Resume get an update might not be sustainable.