1. Implemented user role/authority.
2. Handled more conditions in JWT filter.

----------

Implemented user role/authority:

-User role is now retieved from the role column in the database.

-Role/authority is managed by Spring Security.

-Marked 'tempaccount'-related elements as deprecated. It used to work
similarly to role/authority. We don't need this anymore as we now have
Spring Security's role/authority.


2. Handled more conditions in JWT filter:

-Added more condition handling to 'FilterJWT', where user is already
authenticated (in session) but sends different-identity JWT token with 
the request.
